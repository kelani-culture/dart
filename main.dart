// ~/ is used for integer dividing

// % modulo operator
import 'config.dart';
import 'content.dart';

void main() {
  var config = Config.baseUrl;
  print(config);

  Post post = Post("Post");
  print(post.title);

}
